---
interface Props {
  as?: 'div' | 'section' | 'header' | 'footer' | 'main' | 'article' | 'nav';
  class?: string;
}

const { as = 'div', class: className = '' } = Astro.props as Props;
const Tag = as;
---

<Tag class={`mx-auto w-full max-w-[1180px] px-5 md:px-8 ${className}`}>
  <slot />
</Tag>
